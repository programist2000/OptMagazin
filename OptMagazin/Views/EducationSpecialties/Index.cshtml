@model IEnumerable<OptMagazin.Models.EducationSpecialty>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EducationSpecialtyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EducationSpecialtyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EducationSpecialtyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EducationSpecialtyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EducationSpecialtyId })
        </td>
    </tr>
}

</table>
