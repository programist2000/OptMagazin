@model IEnumerable<OptMagazin.Models.Prize>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PrizeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PrizeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrizeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrizeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrizeId })
        </td>
    </tr>
}

</table>
