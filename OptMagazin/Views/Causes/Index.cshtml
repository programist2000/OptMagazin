@model IEnumerable<OptMagazin.Models.Cause>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CauseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CauseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CauseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CauseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CauseId })
        </td>
    </tr>
}

</table>
