@model IEnumerable<OptMagazin.Models.WorkerPost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Post.PostName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Worker.WorkerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Post.PostName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Worker.WorkerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkerPostId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkerPostId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkerPostId })
        </td>
    </tr>
}

</table>
